import 'package:flutter/material.dart';

// welcome page
 const Color colorCustomWhite = Color.fromARGB(255, 245,245,245);
 const Color colorDarkWhite = Color.fromARGB(255, 224,224,224);


  // title_text_widget and subtitle_widget and authbutton
  const Color colorBlack = Colors.black;
  const Color colorWhite = Colors.white;
  const Color colorCustomBlack = Color.fromARGB(255, 39,50,56);
  const Color colorMedBlack = Color.fromARGB(255, 56,70,78);
  const Color colorLoginNow = Color.fromARGB(255,53,194,193);

  const LinearGradient gradient = LinearGradient(
    begin: Alignment.bottomCenter,
    end: Alignment.topCenter,
    stops: [0.45, 2],
    colors: [
    Colors.orangeAccent,
    Color.fromARGB(255,165,112,34),
    
  ]); 